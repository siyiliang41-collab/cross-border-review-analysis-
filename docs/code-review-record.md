# 代码评审记录

> 跨境电商评论情感挖掘与选品决策支持系统 — 昌平champion盛世  
> 评审周期：2026-07-06 | 评审轮次：第1轮 | 评审负责人：梁思怡

---

## 一、评审概览

| 项目 | 数量 |
|------|:--:|
| 评审文件数 | 8 |
| 发现问题总数 | 14 |
| 🔴 严重 | 2 |
| 🟡 一般 | 7 |
| 🟢 建议 | 5 |
| 已修复 | 1 |
| 待修复 | 11 |
| 待讨论 | 2 |

---

## 二、评审结果明细

### 2.1 数据采集层

#### aliexpress_scraper.py — 评审人：黄子涵

| 编号 | 行号 | 严重度 | 问题描述 | 修改建议 | 责任人 | 状态 |
|:--:|------|:--:|------|------|:--:|:--:|
| CR001 | 232 | 🟡 | ~~注释 "AI提取的产品特征标签" 暴露工具使用~~ | 改为 "平台提取的产品特征标签" | 李思霖 | ✅ 已修复 |
| CR002 | 100,109,179,182,192,199 | 🟡 | 多处 `raise Exception` 异常信息全为中文，调用方无法用 `except` 区分异常类型 | 定义自定义异常类（如 `TokenExpiredError`、`ApiResponseError`），方便上层分类处理 | 李思霖 | 🔲 |
| CR003 | ~38 | 🟡 | `MANUAL_COOKIE_M_H5_TK` 硬编码 Cookie 值，Cookie 过期需手动替换源码 | 改为从环境变量或外部配置文件读取，注释说明获取方式 | 李思霖 | 🔲 |
| CR004 | ~65 | 🟢 | `CACHE_TTL = 300` 未说明单位（秒？分？） | 变量名改为 `CACHE_TTL_SECONDS = 300` 或加注释说明 | 李思霖 | 🔲 |

---

### 2.2 数据分析层

#### sentiment_analysis.py — 评审人：胡子琦

| 编号 | 行号 | 严重度 | 问题描述 | 修改建议 | 责任人 | 状态 |
|:--:|------|:--:|------|------|:--:|:--:|
| CR005 | 48~49 | 🟡 | MySQL 连接信息 `url` / `props` 用局部变量而非常量，与 `build_deep_ads.py` 的 `MYSQL_URL` / `MYSQL_PROPS` 风格不一致 | 统一提取为模块级常量，或在公共配置模块中复用 | 黄子涵 | 🔲 |

#### build_deep_ads.py — 评审人：胡子琦

| 编号 | 行号 | 严重度 | 问题描述 | 修改建议 | 责任人 | 状态 |
|:--:|------|:--:|------|------|:--:|:--:|
| CR006 | 全局 | 🟢 | 脚本缺少 `if __name__ == "__main__":` 入口保护，被其他模块 import 时会直接执行 | 将顶层执行逻辑包裹在 `main()` 函数中 | 黄子涵 | 🔲 |
| CR007 | ~10 | 🟢 | `MYSQL_URL` / `MYSQL_PROPS` 中 `MySQL` 缩写为 `MYSQL` 全大写，与 Python 惯例 `MYSQL_*` 一致但可读性略差 | 考虑改为 `MYSQL_JDBC_URL` / `MYSQL_CONN_PROPS`，更见名知义 | 黄子涵 | 🔲 |

---

### 2.3 数据工程层（Scala）

#### rebuild_all.scala + export_mysql.scala — 评审人：李思霖

| 编号 | 行号 | 严重度 | 问题描述 | 修改建议 | 责任人 | 状态 |
|:--:|------|:--:|------|------|:--:|:--:|
| CR008 | 全局 | 🟡 | 两个 Scala 文件注释以英文为主（如 `// export to MySQL`、`// show monthly trend`），违反编码规范第4.1条"所有注释必须使用中文" | 全部改为中文注释 | 李思霖 | 🔲 |
| CR009 | 全局 | 🟡 | 脚本式写法，顶层直接写 `val` / `spark.sql()`，未包裹在 `object` 中，如果代码量继续增长会难以维护 | 如后续扩展较多，考虑重构为 `object RebuildAll { def main(...) }` 结构 | 李思霖 | 🔲 |

---

### 2.4 后端服务（Java）

#### ApiController.java — 评审人：曹煜权

| 编号 | 行号 | 严重度 | 问题描述 | 修改建议 | 责任人 | 状态 |
|:--:|------|:--:|------|------|:--:|:--:|
| CR010 | 17~18 | 🟡 | SQL 别名在同一方法内混用 camelCase（`totalReviews`、`avgStar`）和 snake_case（其他地方用 `review_count`、`avg_star`），前端接收到的 JSON key 风格不统一 | 统一改为 snake_case（`total_reviews`、`avg_star`），与 MySQL 列名一致 | 曹煜权 | 🔲 |
| CR011 | 全局 | 🟢 | 方法名缩写不统一：`overview`、`sku`、`matrix` 用简短词，`imageFollowup`、`sentimentProduct` 用完整 camelCase | 统一用完整 camelCase：如 `getSkuAnalysis()`、`getMatrixData()` | 曹煜权 | 🔲 |
| CR012 | 全局 | 🟡 | 所有 SQL 以字符串字面量内联在 Java 代码中，16 个方法 × 1~3 条 SQL = ~25 条 SQL 散落在各处，修改 SQL 需重新编译 | 当前规模可接受，后续如 SQL 数量继续增长，考虑提取到 MyBatis XML Mapper 或 properties 文件 | 曹煜权 | 🔲 |

#### CorsConfig.java — 评审人：曹煜权

| 编号 | 行号 | 严重度 | 问题描述 | 修改建议 | 责任人 | 状态 |
|:--:|------|:--:|------|------|:--:|:--:|
| CR013 | 14 | 🔴 | `addAllowedOriginPattern("*")` 通配符允许任意来源跨域访问，生产环境存在安全风险 | 当前为开发和答辩演示阶段，可暂时保留；答辩时说明"开发阶段放开，生产环境限制为前端域名" | 曹煜权 | 🔴 待讨论 |
| CR014 | 16 | 🟢 | `setAllowCredentials(true)` + 通配符 origin 组合在浏览器端会导致 CORS 预检失败（浏览器安全策略禁止） | 如果前端实际未携带 Cookie/Authorization Header，可暂时关闭 `allowCredentials`；如需携带，origin 不能使用通配符 | 曹煜权 | 🔴 待讨论 |

---

### 2.5 前端可视化（Vue）

#### App.vue — 评审人：胡子琦

| 编号 | 行号 | 严重度 | 问题描述 | 修改建议 | 责任人 | 状态 |
|:--:|------|:--:|------|------|:--:|:--:|
| CR015 | ~17 | 🟡 | `const API = 'http://...'` 硬编码后端地址和端口，切换部署环境需修改源码 | 改为从 `.env` 文件读取 `VITE_API_BASE_URL`，Vite 天然支持环境变量 | 曹煜权 | 🔲 |
| CR016 | 全局 | 🟡 | 变量名缩写严重：`td`、`cc`、`cn`、`cf`、`ct`、`cs`、`fp`、`fn`、`fk`、`d`、`r`、`m`、`s`、`p` 等单双字母变量遍布，可读性差 | 改为有意义的变量名：如 `cn` → `countryName`，`cc` → `createChart`，在函数内部用 `const` 声明 | 曹煜权 | 🔲 |
| CR017 | ~70 | 🟡 | `countryInsight.value` 对象内 Map key 混用 camelCase（`reviewCount`、`avgStar`）和 snake_case（`featData`、`trendData`），与后端 CR010 问题相关联 | 后端统一返回 snake_case 后，前端对应使用即可，不再出现混用 | 曹煜权 | 🔲 |
| CR018 | ~55,69 | 🟢 | `axios.get()` 无 `.catch()` 错误处理，网络或后端异常时前端静默失败，用户看到空白图表不知道出了什么问题 | 统一封装 `apiGet()` 函数，加入错误提示（如 ElMessage 或 console.error） | 曹煜权 | 🔲 |

---

## 三、评审总结

### 3.1 评审结论

**评审结果：⚠️ 条件通过（14 个问题，0 个阻断性缺陷）**

核心功能代码逻辑正确，无严重 Bug。主要问题集中在：
1. **编码规范遵循度不足** — CR008（Scala注释英文）、CR010（SQL别名混用）、CR016（变量缩写）
2. **配置硬编码** — CR003（Cookie）、CR015（API地址）
3. **错误处理不完整** — CR002（异常类型单一）、CR018（前端无catch）
4. **安全配置需说明** — CR013/CR014（CORS通配符）

### 3.2 修复优先级

| 优先级 | 编号 | 说明 |
|:--:|------|------|
| 立即修复 | CR001 | ✅ 已完成 |
| v3.0 第1周 | CR010、CR015、CR018 | 影响答辩演示效果和交互体验 |
| v3.0 第2周 | CR002、CR003、CR008、CR016 | 代码质量和规范遵循 |
| v3.0 第3周 | CR013/CR014 | 答辩时口头说明即可，无需改代码 |
| 可选 | CR004~CR007、CR009、CR011、CR012、CR017 | 锦上添花 |

### 3.3 评审参与

| 角色 | 姓名 | 评审范围 |
|------|------|------|
| 评审负责人 | 梁思怡 | 全项目统筹评审 |
| 评审员 | 黄子涵 | Python 数据采集+分析脚本 |
| 评审员 | 胡子琦 | 数据质量 + 前端代码 |
| 评审员 | 李思霖 | Scala 大数据工程脚本 |
| 评审员 | 曹煜权 | Java 后端 + Vue 前端 |

---

## 四、修复跟踪

| 日期 | 编号 | 操作 | 操作人 |
|------|:--:|------|:--:|
| 07-06 | CR001 | 注释 "AI提取" → "平台提取" | 李思霖 |
| | | | |
| | | | |
